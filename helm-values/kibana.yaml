ingress:
  enabled: true
  className: nginx
  pathtype: Prefix
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
  hosts:
    - host: kibana.devdeploy.site
      paths:
        - path: /
  tls:
    - secretName: logging-tls
      hosts:
        - kibana.devdeploy.site